config {
    type: "view"
}

js { 
  const CLMVAR = 30;
  module.exports = { CLMVAR };
}

SELECT
  *, ${funcs.CLMTT("Value")} as FOISDEUX
FROM
  ${ref("CLM_SRC_VIEW")}
  where Value < ${CLMVAR} and
  Value > ${constants.multv}

